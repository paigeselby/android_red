<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="xyz.paigeselby.crazyred" >

    <!--
        <uses-permission android:name="android.permission.MOUNT_FORMAT_FILESYSTEMS"/> 1
     <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS"/> 1.2
    <uses-permission android:name="android.permission.NFC"/> 1.3
    <uses-permission android:name="android.permission.PERSISTENT_ACTIVITY"/> 1.4
    <uses-permission android:name="android.permission.PROCESS_OUTGOING_CALLS"/> 1.5
    <uses-permission android:name="android.permission.READ_CALENDAR"/> 1.6
 1.7

-->
    <uses-permission android:name="android.permission.READ_CALL_LOG"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="xyz.paigeselby.crazyred.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
